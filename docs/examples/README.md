# Example outputs

The reproducible examples are generated from the fixed synthetic snapshot in `data/input`.

Run:

```powershell
$env:INDUSTRIAL_AGENT_OFFLINE_MODE="true"
python demo.py
```

Generated files are written under `runtime/outputs/<request_id>/` and are intentionally ignored by Git. This keeps the repository clean while ensuring every example can be regenerated from source data.

