# Benchmark

`questions.jsonl` is the starter benchmark for deterministic factory questions and safety rejections. Each row defines the expected status, primary dataset, and whether the case is a safety test.

Extend it with at least 25 questions before publishing accuracy metrics. Keep expected evidence identifiers explicit and use synthetic data only.

